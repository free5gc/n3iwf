package service

import (
	"sync"
	"testing"
)

func TestServer(t *testing.T) {

	var wg sync.WaitGroup

	wg.Add(2)

	if err := Run(); err != nil {
		t.Fatalf("Run IKE service failed: %+v", err)
	}

	wg.Wait()

}
